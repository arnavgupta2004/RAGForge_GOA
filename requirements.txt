# Runtime dependencies for the deployed API (Railway). Kept separate from
# requirements-dev.txt so the production image doesn't pull in `datasets`
# (pyarrow etc., only used offline by scripts/download_dataset.py) or the
# test toolchain -- smaller image, smaller memory footprint, matters at
# Railway's 1GB free-trial RAM ceiling (see docs/decisions.md).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
pyyaml==6.0.2
python-multipart==0.0.20
python-dotenv==1.0.1

sentence-transformers==3.3.1
bm25s==0.3.10
pysbd==0.3.4
tiktoken==0.14.0

google-genai==1.67.0
httpx==0.28.1
tenacity==9.0.0

numpy>=1.26,<3
