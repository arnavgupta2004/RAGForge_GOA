# Base URL of the deployed RAGForge backend (FastAPI on Railway).
# No secrets belong here -- this file is public in the browser bundle.
# Local dev default (frontend/src/lib/api.ts) is http://localhost:8420 if unset.
VITE_API_BASE_URL=https://YOUR-RAILWAY-BACKEND.up.railway.app
